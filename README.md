# Image-Classifier-App-CNNproject
This project implements a Convolutional Neural Network (CNN) to classify images into different categories. It preprocesses input images, extracts features through multiple convolutional and pooling layers. The model is trained and tested on labeled datasets, demonstrating deep learning techniques for computer vision applications.
